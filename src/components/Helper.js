import { Icon } from "rsuite";

function Helper() {
  return (
    <span>
      <Icon icon="ban" className="ban-icon" />
      {"  "}
      <strong>Inactive</strong>. Click for activation option.
    </span>
  );
}

export default Helper;
